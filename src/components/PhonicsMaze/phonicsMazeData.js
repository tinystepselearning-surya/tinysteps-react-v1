// Phonics maze data removed. Export minimal helpers so imports continue to work.

export function getPhonicsAdaptiveSettings() {
  return { level: 1, focus: 'CVC', support: 'low', difficulty: 'easy' };
}

export function getOfflineMaze() {
  return null;
}
