// Placeholder skipped test to avoid Vitest treating this file as an empty/invalid suite.
// The real test for useEnrollments is in `useEnrollments.spec.tsx`.
import { describe } from 'vitest';

// Mark as skipped so Vitest reports it but doesn't run any assertions.
describe.skip('placeholder-useEnrollments-ts', () => {});
