// WorksheetGeneratorPage has been removed from production routing.
// Kept minimal stub to avoid accidental imports until fully deleted.
import React from 'react';

export default function WorksheetGeneratorPage() {
  return null;
}
