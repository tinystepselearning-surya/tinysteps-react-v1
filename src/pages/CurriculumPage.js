// Minimal wrapper: re-export the TSX module so the dev server builds from the canonical TypeScript source.
// NOTE: This file intentionally kept tiny. The canonical component is `CurriculumPage.tsx`.
import CurriculumPage from './CurriculumPage.tsx';
export default CurriculumPage;
