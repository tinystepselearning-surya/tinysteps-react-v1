# Brick 7 scope lock

Brick 7 changes only the primary Students scheduling UI and its rolling-schedule/lifecycle routing. It does not implement parent or teacher long-range projections, analytics forecasting, teacher-reassignment reconciliation, or legacy data cleanup. Those remain separate later bricks so this cutover can be validated independently.
