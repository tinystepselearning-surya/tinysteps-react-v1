// SpellBee dataset removed per request — keep a tiny safe placeholder to avoid import errors.
exports.enhancedSpellbeeData = {
  removed: true,
  note: 'SpellBee dataset removed from repository.'
};
