{
  "collectionName": "attendance",
  "documentIdPattern": "{sessionId}/attendanceRecords/{date}",
  "fields": {
    "sessionId": {
      "type": "string",
      "description": "ID of the session this attendance record belongs to."
    },
    "date": {
      "type": "string",
      "description": "Date of the attendance record in YYYY-MM-DD format."
    },
    "studentId": {
      "type": "string",
      "description": "UID of the student."
    },
    "status": {
      "type": "string",
      "enum": ["present", "absent", "late"],
      "description": "Attendance status of the student."
    },
    "notes": {
      "type": "string",
      "description": "Optional notes about the attendance."
    },
    "markedBy": {
      "type": "string",
      "description": "UID of the teacher or LP who marked the attendance."
    },
    "createdAt": {
      "type": "timestamp",
      "description": "Timestamp when the attendance was created."
    },
    "updatedAt": {
      "type": "timestamp",
      "description": "Timestamp when the attendance was last updated."
    }
  },
  "relationships": {
    "sessionId": "sessions/{sessionId}",
    "studentId": "kids/{kidId}",
    "markedBy": "users/{uid}"
  },
  "accessControl": {
    "read": ["admin", "teacher", "lp"],
    "write": ["admin", "teacher"]
  },
  "compositeIndexes": [
    {
      "fields": ["sessionId", "date"],
      "description": "For querying attendance by session and date."
    },
    {
      "fields": ["studentId", "date"],
      "description": "For querying attendance by student and date."
    }
  ],
  "validationConstraints": {
    "status": {
      "enum": ["present", "absent", "late"],
      "description": "Must be one of the predefined statuses."
    },
    "date": {
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Must be a valid date in YYYY-MM-DD format."
    }
  }
}